node_modules/
dist/

# Upstream snapshots, kept only so a failed fetch can fall back to the last
# good copy. Regenerated by `npm run collect`.
.cache/

# Generated data layer. Built fresh on every deploy, so committing it would add
# a twice-daily churn commit for bytes nobody reviews.
public/data/

*.log
.DS_Store
